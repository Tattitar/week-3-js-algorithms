// Algorithm 1 - Convert Age to Days
function calcAge(age) {
    var days = age * 365;
    return days;
}

console.log(calcAge(65));
console.log(calcAge(1));
console.log(calcAge(0));
console.log(calcAge(20));